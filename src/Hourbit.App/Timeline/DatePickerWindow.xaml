<Window x:Class="Hourbit.App.Timeline.DatePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:automation="clr-namespace:System.Windows.Automation;assembly=PresentationCore"
        Width="430" Height="250"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Title="{Binding Title}"
        Closed="OnClosed"
        FontFamily="Segoe UI, Microsoft YaHei UI"
        FontSize="16">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="DatePickerHeading" Text="{Binding Heading}"
                   FontSize="20" FontWeight="SemiBold" />
        <TextBlock x:Name="DatePickerDescription" Grid.Row="1" Margin="0,6,0,12"
                   Text="{Binding Description}" TextWrapping="Wrap" />
        <DatePicker x:Name="DateInput"
                    Grid.Row="2"
                    automation:AutomationProperties.Name="{Binding AccessibleName}"
                    SelectedDateFormat="Short"
                    FontFamily="Segoe UI, Microsoft YaHei UI"
                    FontSize="16"
                    MinHeight="40"
                    VerticalContentAlignment="Center"
                    VerticalAlignment="Top">
            <DatePicker.Resources>
                <Style TargetType="{x:Type Button}" />
            </DatePicker.Resources>
        </DatePicker>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="{Binding CancelText}"
                    MinWidth="76" Margin="0,0,8,0" IsCancel="True" />
            <Button x:Name="ViewButton" Content="{Binding ViewText}"
                    MinWidth="76" IsDefault="True" Click="OnAccept" />
        </StackPanel>
    </Grid>
</Window>
