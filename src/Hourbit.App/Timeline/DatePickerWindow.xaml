<Window x:Class="Hourbit.App.Timeline.DatePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:automation="clr-namespace:System.Windows.Automation;assembly=PresentationCore"
        Title="{Binding Title}"
        Width="390" Height="210"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="DatePickerHeading" Text="{Binding Heading}" FontSize="20" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1" Margin="0,6,0,12"
                   Text="{Binding Description}"
                   TextWrapping="Wrap" />
        <DatePicker x:Name="DateInput"
                    Grid.Row="2"
                    automation:AutomationProperties.Name="{Binding AccessibleName}"
                    SelectedDateFormat="Long"
                    VerticalAlignment="Top" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{Binding CancelText}" MinWidth="76" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="{Binding ViewText}" MinWidth="76" IsDefault="True" Click="OnAccept" />
        </StackPanel>
    </Grid>
</Window>
