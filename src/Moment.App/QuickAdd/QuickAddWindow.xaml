<Window x:Class="Moment.App.QuickAdd.QuickAddWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:automation="clr-namespace:System.Windows.Automation;assembly=PresentationCore"
        Title="时刻"
        Width="680"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        Background="{StaticResource WindowBackgroundBrush}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibility" />
    </Window.Resources>
    <Window.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding SubmitCommand}" />
        <KeyBinding Key="Tab" Command="{Binding ToggleDetailsCommand}" />
        <KeyBinding Key="Escape" Command="{Binding HideCommand}" />
    </Window.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox x:Name="InputBox"
                 Grid.Row="0"
                 Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
                 FontSize="25"
                 MinHeight="66"
                 Margin="22,22,22,12"
                 VerticalContentAlignment="Center"
                 automation:AutomationProperties.Name="快速创建提醒输入" />

        <Border Grid.Row="1"
                Margin="22,0,22,12"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="5"
                Visibility="{Binding IsChoicePanelVisible, Converter={StaticResource BooleanToVisibility}}">
            <StackPanel>
                <TextBlock Text="{Binding GuidanceText}"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Margin="16,15,16,12" />
                <ItemsControl ItemsSource="{Binding Choices}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource ChoiceButton}"
                                    Content="{Binding Label}"
                                    Command="{Binding DataContext.ChooseCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                    CommandParameter="{Binding}"
                                    automation:AutomationProperties.Name="{Binding Label}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Text="选择时间后可按 Enter 创建"
                           Foreground="{StaticResource SecondaryTextBrush}"
                           Margin="16,12,16,15" />
            </StackPanel>
        </Border>

        <Border x:Name="PreviewPanel"
                Grid.Row="2"
                Margin="22,0,22,12"
                Padding="16,14"
                MinHeight="60"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="5">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding PreviewText}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding PreviewText}" Value="">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="▣" FontFamily="Segoe UI Symbol" FontSize="21"
                           VerticalAlignment="Center" Margin="0,0,14,0" />
                <TextBlock Text="{Binding PreviewText}" FontSize="17" VerticalAlignment="Center" />
            </StackPanel>
        </Border>

        <Border Grid.Row="3"
                Margin="22,0,22,12"
                Padding="14,10"
                Background="#FFF7F6"
                BorderBrush="#F3C9C3"
                BorderThickness="1"
                CornerRadius="5">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource MissedBrush}" />
        </Border>

        <Border Grid.Row="3"
                Margin="22,0,22,12"
                Padding="14,10"
                Background="{StaticResource SubtleBackgroundBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="5"
                Visibility="{Binding AreDetailsVisible, Converter={StaticResource BooleanToVisibility}}">
            <TextBlock Text="{Binding PreviewText}" Foreground="{StaticResource SecondaryTextBrush}" />
        </Border>

        <Border Grid.Row="4"
                Background="{StaticResource SubtleBackgroundBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="22,15">
            <TextBlock Text="{Binding FooterText}"
                       Foreground="{StaticResource SecondaryTextBrush}"
                       HorizontalAlignment="Center" />
        </Border>
    </Grid>
</Window>
