<Window x:Class="Moment.App.Timeline.DatePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:automation="clr-namespace:System.Windows.Automation;assembly=PresentationCore"
        Title="选择日期"
        Width="390" Height="210"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="查看哪一天？" FontSize="20" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1" Margin="0,6,0,12"
                   Text="选择未来日期后仍保留当前的日、周或月模式。"
                   TextWrapping="Wrap" />
        <DatePicker x:Name="DateInput"
                    Grid.Row="2"
                    automation:AutomationProperties.Name="查看日期"
                    SelectedDateFormat="Long"
                    VerticalAlignment="Top" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" MinWidth="76" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="查看" MinWidth="76" IsDefault="True" Click="OnAccept" />
        </StackPanel>
    </Grid>
</Window>
